/*$(document).ready(function() {
    // Realizar la llamada asíncrona para obtener el contenido
    $.ajax({
        url: "{{ route('productos') }}",
        type: 'GET',
        success: function(response) {
            // Mostrar el contenido una vez que se haya cargado

            // IMPLEMENTAR LUEGO 

            // $('#contenedorOfertas').html(response);
            // $('#contenedorOfertas').fadeIn(); // Mostrar el contenido con un efecto de fundido
        },
        error: function(xhr, status, error) {
            // Manejar el error si la llamada falla
            console.error(error);
        }
    });
});

*/
