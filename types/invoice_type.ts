export const enum DISCOUNT_TYPE {
    FIXED = 'FIXED',
    PERCENTAGE = 'PERCENTAGE',
    NONE = 'NONE'
}

export const enum STATUS {
    CREATED = 'CREATED',
    PAID = 'PAID',
    CANCELLED = 'CANCELLED',
    RECURRING = 'RECURRING'
}