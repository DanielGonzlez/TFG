export const enum DISCOUNT_TYPE {
    FIXED = 'FIXED',
    PERCENTAGE = 'PERCENTAGE'
}

export const enum STATUS {
    CREATED = 'CREATED',
    PAID = 'PAID',
    CANCELLED = 'CANCELLED',
    RECURRING = 'RECURRING'
}